# Django
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Database — set USE_SQLITE_FALLBACK=0 to switch to SQL Server
USE_SQLITE_FALLBACK=1
DB_NAME=hangman_db
DB_USER=sa
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=1433
DB_DRIVER=ODBC Driver 18 for SQL Server
