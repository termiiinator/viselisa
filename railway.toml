[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py seed_players && gunicorn backend_project.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
restartPolicyType = "on_failure"
